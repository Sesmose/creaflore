{% extends 'base.html.twig' %}

{% block title %}Tous nos bouquets{% endblock %}

{% block body %}
<div class="container mt-4">

    <h1>Tous nos bouquets</h1>

    <div class="row">

    {% for bouquet in bouquets %}

    <div class="col-md-4">
        {% include 'bouquet/bouquets.html.twig'%}
    </div>
    {% endfor %}

    </div>

        <div class="pagination">
        
{{ knp_pagination_render(bouquets) }}
    
    </div>
</div>

{% endblock %}